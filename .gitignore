# No trailing slash on .venv / certs, deliberately: `foo/` matches a directory
# but NOT a symlink to one, and scripts/new_worktree.sh links both into every
# worktree. With the slash they showed as untracked there, which made
# rm_worktree.sh's "would this lose work?" check refuse every single time —
# training --force as the routine path and destroying the check. Matching the
# main tree's real directories is unaffected.
.venv
__pycache__/
*.py[cod]
*.egg-info/
.env
.env.*
!.env.example
venv/
*.log

# macOS metadata
.DS_Store
**/.DS_Store

# Claude Code local settings — not project config
# ...except the slash commands and settings.json. Both are project tooling with no
# personal data and no secrets, they get edited during development like any other
# source file, and ignoring them has already cost us one: the original troubleshoot
# command existed only in a chat transcript and had to be rebuilt by hand.
# settings.json carries the project's hooks — the agent-tool guard depends on it, and
# an enforcement mechanism that lives only on one machine is not enforcement.
# settings.local.json stays ignored — machine-specific paths.
#
# Note the shape: ".claude/" alone cannot be used, because git will not descend
# into an excluded directory and a "!" rule below it never matches. Exclude the
# contents, re-admit the one subdirectory, then its .md files.
.claude/*
!.claude/commands/
.claude/commands/*
!.claude/commands/*.md
!.claude/settings.json
# .claude/rules/ admitted 2026-08-14, same reasoning as settings.json above and
# for a sharper reason: `git worktree add` checks out tracked files only, so an
# ignored rules directory is absent from every worktree. That would have silently
# re-created the exact bypass the 08-14 context-gate fix closed -- `/backlog
# attack` workers, the thinnest-context sessions there are, getting no area rules
# at all. Caught before the commit, not after.
!.claude/rules/
.claude/rules/*
!.claude/rules/*.md

# ---------------------------------------------------------------------------
# CREDENTIALS — never committed.
#
# `vertex-key.json` is a GCP service account key with live Vertex AI access. It sat
# UNIGNORED and untracked on the VM until 2026-08-15, showing as `??` in git status —
# one `git add -A` from being committed and pushed to a GitHub remote. CLAUDE.md has
# always listed it in the Denied row, but that governs what a Claude Code session may
# edit; it does nothing to stop git. Being told is not being prevented — the same class
# as the unenforced tool allowlists and the `_PROMPT_EXCLUDED` set that enforced nothing.
#
# The gap was structural, not an oversight about one file: this file's data rules are
# thorough (every persona tree, every per-persona setting) while its credential rules
# were incidental — .env and caldav.yaml were each added when someone happened to think
# of them. So these are patterns, not filenames. Nothing tracked matched them when they
# were added, checked with `git ls-files`.
#
# Precedent for the cost: the 2026-07-29 history rewrite.
vertex-key.json
*-key.json
*credentials*.json
*.pem
*.p12
service-account*.json

# TLS certs — never committed (IP-specific; regenerate with mkcert on each machine)
# No trailing slash — see the .venv note at the top of this file.
certs
certs_backup/

# Android build — Capacitor generated; regenerate with: npx cap add android
#
# ⚠ EXCEPT THE MANIFEST, WHICH IS HAND-EDITED AND REGENERATION DESTROYS IT (2026-09-04).
# AndroidManifest.xml carries ACCESS_COARSE_LOCATION / ACCESS_FINE_LOCATION, added after the
# first real location ping failed with "could not get a location fix" and no permission prompt
# — Android refuses navigator.geolocation at the OS level without them, silently. Those lines
# are not reproducible by `npx cap add android`; it writes the default four-permission manifest
# and location dies again with no error anywhere. Un-ignoring needs every parent directory
# re-admitted, which is why this is six lines rather than one.
# scripts/check_apk_sync.sh also asserts both permissions in the BUILT APK, so a regeneration
# that slips past git is still caught at build time.
android/*
!android/app/
android/app/*
!android/app/src/
android/app/src/*
!android/app/src/main/
android/app/src/main/*
!android/app/src/main/AndroidManifest.xml

# Node dependencies
node_modules/

# Sensitive data — never committed
data/logs/*.json
data/journal/*
data/wisdom/*
data/archive/*
data/memory/*

# Runtime data (sensitive or ephemeral)
data/audio/
data/baselines/
data/config/
data/context.json
data/conversations/
data/push_subscriptions.json
data/voices/

# ---------------------------------------------------------------------------
# REAL USER personas — sensitive tier. Never committed, under any circumstances.
#
# `mike` is a real person's logs, journal, health, finances and memory. This is
# not a "test persona" and never was, though the heading above these lines said
# so until 2026-08-04. There is NO `git add -f` escape hatch here: if something
# in this tree looks like it needs committing, the answer is that it does not.
# Sensitive-tier data does not enter git history — that is the 2026-07-29
# incident, which cost a full history rewrite.
data/personas/mike/

# ---------------------------------------------------------------------------
# ALL OTHER persona data trees — synthetic and dev personas.
#
# This was an enumerated list until 2026-08-04, and the list fell behind:
# pepys, test_a3, sarah_chen and three of ryan_holiday's subdirectories were
# covered, while arthur_brooks, cal_newport, danny_park, maya_torres and
# oliver_burkeman were not. Those five are written to on every validation run
# and a `git add -A` would have committed them. A list that must be updated by
# hand each time a persona is added is the defect, not the missing entries.
#
# The rule that does not drift: no persona data tree is committable by default,
# because the running system writes into all of them and nothing at runtime
# distinguishes a synthetic persona from a real one (persona unification,
# 2026-07-28 — see ROADMAP.md § Binding privacy ruling). The wildcard also
# covers `mike` a second time, which is intended: the explicit rule above is
# there to be read, not because the glob needs help.
#
# Seed fixtures already tracked stay tracked — .gitignore does not untrack.
# A genuinely new *synthetic* fixture needs `git add -f`. Before forcing, check
# which tree you are in: a fixture that silently fails to commit is recoverable,
# a real persona's logs that silently do commit are not.
data/personas/*/

# Real user persona config — sensitive
config/personas/mike.md
config/personas/mike/

# CalDAV credentials — sensitive
config/modules/caldav.yaml

# Session verbatim transcripts — large; source of truth is ~/.claude/projects/*.jsonl
archive/sessions/*.txt
archive/transcripts/

# Per-persona settings (profile, schedule, calendar credentials) — sensitive
config/personas/*/profile.yaml
config/personas/*/scheduler.yaml
config/personas/*/caldav.yaml
# write_config keeps the previous version of prime_directive.md / mission.md
# before overwriting. Tier 1-2 content — sensitive, never committable.
config/personas/*/*.bak

# Pre-reset data archives — user data, never committed
data/_pre_reset_*/

# Diagnostic captures pulled out of live data — may contain real user entries
data/diagnostics/

# Local backups pulled from the VM — real user data, never committed
backups/

# Local-only working files: review sheets, scratch notes, anything holding persona
# data that must never be committed. Added 2026-09-04 for the wisdom-store review —
# the alternative was writing personal data into archive/plans/, which IS committed.
local/

# deploy.sh's mutual-exclusion lock — runtime state, one machine only
.deploy.lock/

# Local convenience file (empty, clicked to reduce editor context noise)
dummy
.dev_backlog_seen

# Intake eval fixtures — real messages from the user's own inbox, hand-labelled.
# Personal data by definition; the eval harness (tests/run_intake_eval.py) is
# tracked, its corpus never is.
tests/intake_fixtures/

# STT benchmark fixtures — 1.6MB of synthesized audio, regenerable with
# `python3 tests/bench_whisper_stt.py --generate`. Not worth carrying in git.
tests/fixtures/stt/

# Plan-scoped deny lift (scripts/hook_deny_lift.py) — a per-plan work order,
# not project state; committing one would ship a live lift to the VM.
.claude/deny_lift.json
