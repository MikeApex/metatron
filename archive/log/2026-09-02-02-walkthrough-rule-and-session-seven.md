### 2026-09-02, second (post-archive rulings — the walkthrough rule, session ⑦, and Cluster H stops waiting) — `CLAUDE.md` (new § Mike-gated work, **307/300**), `archive/plans/capstone_cluster_review_2026-08-27.md`, `DEV_BACKLOG.md`, `SESSION.md`, two new handoffs — **dev-context only, no deploy**

**Three rulings from Mike immediately after the ④ close-out, applied in the same sitting.**

**1. (M) items get walkthroughs, not waits — now a binding rule in root `CLAUDE.md`.** Anything
only Mike can do closes in a guided session Claude proposes and prepares; a standing (M) item
with no proposed session is the named failure mode. First application staged the same hour:
`archive/handoffs/2026-09-02-mike-walkthrough-session-prompt.md` (corpus labelling due 09-09,
wisdom-store review, Darwin key, location zones+APK+ping, BigQuery export, Restic decision).
**Cost of the rule: `CLAUDE.md` is now 307 against its 300 ceiling** — the 08-27 fragment
predicted "the next binding rule forces a restructure," and it has. The restructure was
deliberately NOT done inline (compression pressure mangling rationale is the documented
failure); it is checked at session ⑦ and what moves out is Mike's call. Chat Archiving was
compacted by 3 lines (pointers it already carries in `docs-and-logs.md`); nothing else trimmed.

**2. The three never-scheduled items fold into a new session ⑦ rather than being re-homed.**
Overrides the ④ close-out's recommendation (provenance in, `[DB-0804-02]` → Track B,
`[DB-0808-06]` postponed): Mike wants all three inside the capstone. ⑦'s prompt
(`…session-seven-capstone-remainder-prompt.md`) takes `[DB-0818-08]` as its centrepiece,
`[DB-0804-02]`'s buildable-now slice (the E1-gated rest re-homes to Track B in-session, with
his word), and `[DB-0808-06]` as design-first. **The capstone now closes at ⑦'s end**, not ⑥'s
— ⑥'s prompt amended accordingly.

**3. Cluster H rewritten to the ruled reality** — no longer "Mike to give final ruling":
departures recorded (0822-08 closed, 0827-09 graduated, 0810-11 → rebuild notebook, 0814-03
parked post-capstone), the remaining ten confirmed parked, and 0818-08's move INTO the close
path noted.

Rejected: doing the CLAUDE.md restructure now (rationale-trimming risk, and what moves is his
call); putting the three ⑦ items into ⑤/⑥ (provenance alone is a half-day — overloading the
referent session risks both).
