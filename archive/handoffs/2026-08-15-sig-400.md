# Handoff — 2026-08-15, worktree `wt/sig-400` — [DB-0810-12] thought_signature 400

**Shipped.** `_openai_compat_stream` no longer leaves an assistant message carrying unsigned
function calls in `messages` for a later Vertex turn to reject. Where a signature is demanded
(Google endpoints only — `base_url` contains `googleapis`), the turn is recorded *signature-free*
instead: an assistant text message plus a user `[tool results]` message naming the tools that
already ran. Nothing for Vertex to validate, tools still dispatched once, the conversation
continues to its final turn — the exchange survives, which was the reported harm. OpenAI and
Ollama keep byte-identical behaviour; they never demanded a signature.

**Both unsigned routes are closed, not just the evidenced one.** The `else` /
`src=stream_delta_fallback` branch was the one the 08-15 capture named. The second candidate the
item flagged and nothing had checked — `blocking_replay[...]` — was real: the code appended
`_signed` and *then* classified it, so an unsigned replay was already in `messages`. The check now
runs before the append and takes the same signature-free route. **Third route ruled out by
inspection:** `history` is the only other source of messages and every append to it anywhere in
`core/` is a plain `{role, content}` pair — no `tool_calls` can enter that way. `_run_gemini_native_loop`
(candidate (b)) is untouched and still unguarded, as the item de-prioritised it.

**Rejected:** porting `_openai_compat_loop`'s tc0-only workaround (serialises parallel dispatch —
the documented regression); dropping the turn from history (model re-issues the calls, side effects
repeat); retrying the blocking call (speculative, and the divergence is not known to be transient).

**Instrumentation survives.** `_note_unsigned()` / `_thought_signature_state()` and every call site
are intact. A neutralised turn is logged with a `:neutralized` suffix on the `src`, so a future 400
still names the branch *and* says the loop wrote no function-call part at that position — which
would point the next investigation elsewhere rather than at a branch that is now inert.

**Commit:** `d180c81` — `core/orchestrator.py`, `tests/test_thought_signature.py`.

**Tests:** `python3 tests/test_thought_signature.py` — 12/12 pass, offline, no credentials. The fake
client reproduces Vertex: it 400s any request containing an unsigned function-call part.
**Verified discriminating** — stashing only `core/orchestrator.py` and re-running gives 4 failures,
including the reproduction raising `Error code: 400 … at position 2` exactly as live. The other 8
pass against both trees, which is what a behaviour-preservation test should do.
`scripts/qa_sweep.sh` 9/9; `tests/test_action_provenance.py` 10/10.

**Proven vs. hypothesis.** Proven by test: no unsigned function-call part reaches a later request
from either branch; the signed path is unchanged (same message object sent back verbatim); parallel
calls stay one turn (2 stream turns for 2 calls, not 3); OpenAI/Ollama unchanged; the classifier is
correct on signed / unsigned / partial / dict-form / hostile input. **Hypothesis:** that Vertex
accepts the signature-free assistant+user pair as a valid continuation, and that the model does not
re-issue the tools despite being told they ran. Both are standard OpenAI-compat message shapes and
neither is verifiable without a live occurrence. Also unverified: whether the `blocking_replay`
route ever fired in production — the ledger has never recorded it, so the fix there is precautionary.

**Do not close [DB-0810-12] on this alone.** The failure is ~4/fortnight and rare by construction;
closing evidence is a live deploy plus a fortnight with no `loop=openai_compat_stream` signature 400
and, better, one `:neutralized` ledger line proving the branch fired and did not 400.

**SESSION.md must carry:** `core/orchestrator.py` changed and **is not deployed** — needs
`./deploy.sh` to the VM. The item moves from UNBLOCKED to *fix landed, awaiting live confirmation*.

**Nothing to correct in the item's description** — it matched the code at every point, including
the `else`-branch location, the `:replay[...]` label logic and the tc0-only regression argument.

**Transcript not captured:** `archive_chats.py` derives its slug from the git root, and the worktree
path has no JSONL of its own (this session's lives under the parent project's slug). It exited
without writing. The coordinator needs to run it from the main tree.
