# 2026-08-15 — [DB-0810-15] STT language plumbing (voice worker)

**Shipped:** `core/voice_pipeline.py` — hardcoded `language="en"` (line 177) replaced with
`WHISPER_LANGUAGE = os.getenv("METATRON_WHISPER_LANGUAGE", "en")`, same env-override pattern as
`WHISPER_MODEL_SIZE`/`WHISPER_BEAM_SIZE`. Default unchanged — verified `WHISPER_LANGUAGE == "en"`
and `WHISPER_MODEL_SIZE == "base.en"` with no env set (before/after identical to pre-change).
With `METATRON_WHISPER_LANGUAGE=bg` set, `WHISPER_LANGUAGE` resolves to `"bg"`; model size stays
`"base.en"` untouched.

`tests/bench_whisper_stt.py` — added Bulgarian fixture set, per-language fixture config
(`_LANGUAGES`), `"lang"` key on every `_CONFIGS` row (all pre-existing rows default `"en"`,
sweep unchanged), two new rows (`base`/en, `base`/bg), Unicode-aware `_normalise()` (Cyrillic was
being stripped to nothing by the old `a-z0-9` regex, which would have zeroed every Bulgarian WER),
and `--language`/`--languages` CLI flags.

**Verified:** `py_compile` + `ast.parse` on both files; `scripts/qa_sweep.sh` 9/9; imported
`core.voice_pipeline` with no env set and with `METATRON_WHISPER_LANGUAGE=bg` set and printed the
resolved constants (see above); ran the bench script's config-filter logic and `_normalise()` on
Cyrillic text directly — correctly tokenizes instead of stripping to empty. Did not run real
transcription or download models.

**NOT closed — [DB-0810-15] stays open.** Two things remain, neither done here:
1. The VM benchmark to decide if `base` (multilingual) is affordable on the 2-vCPU single-worker
   STT pool. Run on the VM: `python3 tests/bench_whisper_stt.py --generate --language bg` (Mac,
   needs network) → scp fixtures to VM → `python3 tests/bench_whisper_stt.py --models base
   --languages en,bg` on the VM.
2. The auto-detect-vs-toggle design decision — Mike's call, not made here.

**Commit blocked:** even `git add core/voice_pipeline.py tests/bench_whisper_stt.py` (not just
`git commit`) fails under `scripts/hook_commit_guard.py` — "COMMIT GUARD: blocked... Path
expressions this guard could not account for: core/voice_pipeline.py,
tests/bench_whisper_stt.py" — reproduced twice, identically. This is despite this worktree being
based on `6ad3dec`, the commit-guard-worktree fix noted as landed today. Did not force past it
with `METATRON_COMMIT_GUARD=off` per instructions. Both manifest files are modified but
**uncommitted** in `/Users/md-homefolder/Desktop/metatron-wt-db081015-voice` — safe, not staged,
nothing lost. Needs either a manual `git add`/commit by someone who can diagnose the guard, or a
further fix to `scripts/hook_commit_guard.py` (not in my manifest — did not touch it).

**For SESSION.md:** DB-0810-15 partially addressed (language plumbing + bench sweep extended,
both uncommitted pending the commit-guard issue above); model swap and auto-detect decision still
open; VM benchmark command above is what unblocks the model decision.
