# 2026-08-15 — [DB-0810-12] thought_signature 400: instrumentation only

**Shipped:** instrumentation in `core/orchestrator.py`. No behavioural change — the bug is
deliberately not fixed, because two prior diagnoses were wrong and a third guess would survive
every test at ~4 occurrences/fortnight.

- `_thought_signature_state()` (`:2136`) — classifies an assistant message `signed` /
  `unsigned` / `signed=i/n` / `n/a`; never raises, never mutates.
- Per-call ledger `_unsigned_appends` + `_note_unsigned()` in `_openai_compat_stream`
  (`:2702`) — records `pos=/turn=/src=/tools=` for every unsigned assistant message this
  loop writes into `messages`, and emits `WARNING [signature_probe] unsigned_assistant_appended`.
- Two `src` values: `stream_delta_fallback` (`:2881`, the leading-hypothesis `else` branch) and
  `blocking_replay[<state>]` (`:2867`, the case where the mitigation itself returns unsigned
  calls — a path nobody had checked).
- Ledger replayed into both `_log_api_failure` sites (`:2740` main stream, `:2835` replay) as
  `msgs=N unsigned=[...]`, matching the existing `msgs=` convention in `_openai_compat_loop`.

**Why decisive on one occurrence:** Vertex names the *position* of the offending message
(position 12 in all four captures). If that position matches a ledger entry, the branch is
named. If `unsigned=[none]`, the hypothesis is falsified and the next investigation goes to
`history` / cross-invocation state instead.

**Verified:** `py_compile`, `qa_sweep.sh` 9/9, and a throwaway probe (`/tmp/db081012_probe.py`,
not in repo) that drove `_openai_compat_stream` with a fake OpenAI client through five
scenarios — else-branch, signed replay, unsigned replay, parallel-signed, parallel-unsigned.
The two "expect no entry" negatives stayed silent, so the probe is low-noise as required.

**Also found:** the `_openai_compat_stream` docstring claimed the Synthesizer "never calls
tools" and that the tool-call path is not exercised. All four captures are synthesizer +
`write_quality_event`, so that was false and actively misleading; comment corrected in place.

**Backlog:** do NOT close [DB-0810-12]. Add: instrumentation landed, awaiting one live
occurrence. **Needs `./deploy.sh` to the VM to be of any use** — not deployed by this session.

**Commit:** see `git log` on `wt/db081012-signature`.
