# Red session ④ — launch prompt (Mike to review before use)

Model: Fable 5. Red-tier judgement work — the model split (build in Opus) does not apply here,
and Red work is never delegated to a subagent.

**Run on or after 2026-08-30**, once all of 2026-08-29's scheduled runs (through the 20:00
`evening_close`) exist in traces. Running earlier voids step 1's evidence.

---

/metatron-code Red session ④ — likely the last of the capstone. Mike present. This session
decides and (if warranted) builds against the re-measure day's evidence, drains the remaining
clock-gated confirms, and closes with the capstone review. Work in order, stopping at anything
decision-shaped:

1. **The re-measure ([DB-0822-08]'s evidence).** Run the procedure in
   `archive/handoffs/2026-08-29-re-measure-and-0822-08-decision.md` over 2026-08-29's full
   scheduled-run day of `mike` traces: per raised item, proposal attached or merely reported
   (baseline: 6-of-9 and 7-of-9 raises, zero proposals). The same day's traces confirm the
   ritual halves ([DB-0809-02]: a question asked at most once; empty-delta runs short; virtues
   only at 20:00) — score both while reading once.

2. **Put the [DB-0822-08] decision to Mike with the numbers.** The handoff frames it:
   instruction fix (recommended against on the item's own reasoning), the structural
   per-run gate (`_synth_conditional_sections()` pattern — "every raised item arrives with a
   proposed next action or is not raised"), or nothing if the post-audit file now proposes.
   If Mike rules for the gate: the injection is core code (Amber), any `synthesizer.md`
   wording is Red and prompts him per the ritual. A4 clinical + pipeline gates re-run after
   any synthesizer change.

3. **Drain the clock-gated confirms against the same day's evidence, closing each in
   `DEV_BACKLOG.md` with its evidence:** scheduler-only journal day stayed empty + answered
   check-in journaled ([DB-0822-05]); one morning-resolved item not re-reported "still
   missing" ([DB-0822-06]); no new empty-label quality events ([DB-0827-07]); the 08-29
   decline stayed declined through scheduled runs ([DB-0827-01] — the live decline landed
   13:05:53, verify nothing re-raised it); afternoon turn carried no virtues (confirmed
   2026-08-29) + the 20:00 run still did ([DB-0822-10]); first 05:45 accountability-gate run
   ([DB-0827-09] — check journalctl or `accountability/verdicts.jsonl`; its Sunday-retro half
   waits for 08-31). Intake went live 2026-08-29 13:54 — if an interest-level email has
   arrived since, check it surfaced with coordination legs ([DB-0822-09]).

4. **One parked Red line rides along:** `synthesizer.md` gained no `source` line for
   write_persona proposals — flagged in `archive/handoffs/2026-08-28-write-persona-gate.md`
   ("schema does the work; Red session may add one"). Put it to Mike as add-or-decline-forever;
   either answer closes the flag.

5. **Then the capstone close-out review with Mike** against
   `archive/plans/capstone_cluster_review_2026-08-27.md`: what in this rendition is done,
   what carries, and whether anything continues at all — the parked decisions (the 08-29
   Inbox triage table, the three @session items, machine-log leftovers: two calendar
   duplicate pairs + Mousetrap, the intake corpus labelling) are ruled here or explicitly
   parked with a date.

No new Red proposals in this session — it spends existing evidence, it does not open fronts.
/archive at close.
